[project]
name = "text-clustering-via-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.42.0",
    "langchain>=0.3.14",
    "langchain-community>=0.3.14",
    "langchain-ollama>=0.2.2",
    "numpy>=2.2.1",
    "openai>=1.59.6",
    "python-dotenv>=1.0.1",
    "scikit-learn>=1.6.0",
    "tqdm>=4.67.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]

[tool.uv.pip]
system = true
